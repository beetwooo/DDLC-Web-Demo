// button controls
function setupControls() {
  const controls = {
    triangle: document.getElementById('btn-triangle'),
    history: document.getElementById('btn-history'),
    skip: document.getElementById('btn-skip'),
    auto: document.getElementById('btn-auto'),
    save: document.getElementById('btn-save'),
    load: document.getElementById('btn-load'),
    settings: document.getElementById('btn-settings'),
  };

  if (controls.triangle) controls.triangle.addEventListener('click', () => {

  });
  if (controls.history) controls.history.addEventListener('click', () => {

  });
  if (controls.skip) controls.skip.addEventListener('click', () => {

  });
  if (controls.auto) controls.auto.addEventListener('click', () => {

  });
  if (controls.save) controls.save.addEventListener('click', () => {

  });
  if (controls.load) controls.load.addEventListener('click', () => {

  });
  if (controls.settings) controls.settings.addEventListener('click', () => {
// todo
  });
}

window.addEventListener('DOMContentLoaded', () => setupControls());